name: OSSF Scorecard

# Publishes an OpenSSF Scorecard supply-chain security score for the
# repo (badge-able, and surfaced in GitHub's security tab via SARIF).
# Scorecard's checks only produce meaningful results on a public repo.
on:
  push:
    branches: [main]
  schedule:
    - cron: "27 3 * * 2"
  workflow_dispatch:

permissions: read-all

jobs:
  analyze:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v4.2.2
        with:
          persist-credentials: false

      - name: Run Scorecard analysis
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc  # v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload SARIF to code scanning
        uses: github/codeql-action/upload-sarif@4187e74d05793876e9989daffde9c3e66b4acd07  # v3.29.0
        with:
          sarif_file: results.sarif
