// Pure utility helpers shared by other tool modules (currently just
// `ToolResults`, guarding `@Tool`-annotated methods against ever
// returning a blank String — see its Javadoc). Deliberately has zero
// dependencies of its own: no `core`, no Spring AI.
